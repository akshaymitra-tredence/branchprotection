name: Gate Approval Workflow

on:
  workflow_run:
    workflows: ["Deploy to Dev Environment"]
    types:
      - completed
    branches:
      - dev

jobs:
  gate-approval:
    runs-on: ubuntu-latest
    steps:
      - name: Output Hello World
        run: |
          echo "hello worlds new commit"